---
import { UI, type Lang } from '../i18n';

interface Props {
	date: Date;
	lang: Lang;
}

const { date, lang } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(UI[lang].dateLocale, {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
